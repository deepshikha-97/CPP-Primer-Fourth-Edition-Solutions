#include <iostream>

using std::cout;
using std::endl;

int main()
{
	if(ival < minval)
		minval = ival;
		occurs = 1;
	//Error: Curly braces are required after if to execute the statement as expected. Programmatically, no error.
}

