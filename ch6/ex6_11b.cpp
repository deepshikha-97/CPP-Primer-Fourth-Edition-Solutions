#include <iostream>
#include <vector>

using std::cout;
using std::cin;
using std::endl;
using std::vector;
using std::string;

int main()
{
	vector<int> ivec(10,0);
	while(vector<int>::iterator iter != ivec.end()) //Conditional operator (!=) is used in the definition of variable.
	{
	}
	return 0;
}

